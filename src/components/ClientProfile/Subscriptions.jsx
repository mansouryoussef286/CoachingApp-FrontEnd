import React from "react";
import {Router, Link} from "react-router-dom"

export default (props)=>{
    return(
        // Container
        <div className="container-fluid col-9 ClientItemContainer shadow d-flex flex-column justify-content-between align-items-center">
          Cards Here
        </div>
    );
}
